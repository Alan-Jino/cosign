name: Docker Image CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:

    runs-on: ubuntu-latest
    env:
      REGISTRY: ghcr.io
      GH_URL: https://github.com
    steps:
    - name: Check out GitHub repo
      uses: actions/checkout@v3
    - name: Login to GitHub Container Registry
      uses: docker/login-action@v2
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GIT_TOKENS }}
    - name: Build image and push to Docker registry
      uses: docker/build-push-action@v4
     
    - name: Build image and push to GitHub Container Registry
      uses: docker/build-push-action@v4
      with:
        push: true
        tags: ${{ env.REGISTRY }}/${{ github.repository }}
    - name: Install cosign
      uses: sigstore/cosign-installer@v3.1.1
    - name: Sign the images
      run: |
          cosign sign -y ${{ env.REGISTRY }}/${{ github.repository }}
      env:
        COSIGN_EXPERIMENTAL: 1
    - name: Verify the pushed tags
      run: cosign verify ${{ env.REGISTRY }}/${{ github.repository }} --certificate-identity ${{ env.GH_URL }}${{ github.repository }}/.github/workflows/docker-image.yml@refs/tags/${{ env.RELEASE_VERSION }}  --certificate-oidc-issuer https://token.actions.githubusercontent.com
      env:
        COSIGN_EXPERIMENTAL: 1
            
    